export default function ModelPage({ params }: { params: Promise<{ slug: string }> }) {
  return (
    <main className="min-h-svh flex items-center justify-center">
      <p className="text-text-3">Model page — coming soon</p>
    </main>
  )
}
